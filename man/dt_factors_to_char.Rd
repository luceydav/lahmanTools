% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{dt_factors_to_char}
\alias{dt_factors_to_char}
\title{Convert factor columns to character in a data.table}
\usage{
dt_factors_to_char(dt)
}
\arguments{
\item{dt}{A \code{data.table}. Modified by reference; no return value needed.}
}
\value{
\code{dt} invisibly.
}
\description{
Converts all factor columns in a \code{data.table} to character in-place (by
reference). Prevents DuckDB from inferring incompatible per-table ENUM
domains for the same column name, which causes cross-table join failures.
}
\examples{
library(data.table)
dt <- data.table(x = factor(c("a", "b")), y = 1:2)
dt_factors_to_char(dt)
class(dt$x)  # "character"
}
